{
  "watch": ["src", "tests"],
  "ext": "ts",
  "ignore": ["node_modules", "dist", ".git"],
  "exec": "rimraf dist && tsc -p tsconfig.json && node ./dist/api/main/server.js",
  "env": {
    "NODE_ENV": "development"
  }
}
