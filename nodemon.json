{
  "watch": ["src", "tests"],
  "ext": "ts",
  "ignore": ["node_modules", "dist", ".git"],
  "exec": "rimraf dist && tsc -p tsconfig.json && node ./dist/api/bootstrap/server.js",
  "delay": "1000",
  "env": {
    "NODE_ENV": "development"
  }
}
